<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jeecg.modules.mes.mapper.ProductionPlanMapper">
  <update id="updatePlanStatus" parameterType="java.lang.String">
            UPDATE mis_production_plan
            SET plan_status = #{planStatus}
            WHERE id = #{planId}
            AND plan_status = '0'
            AND del_flag='0'
    </update>
    <update id="updatePlanStatusBatch">
        UPDATE mis_production_plan
        SET plan_status = #{planStatus}
        WHERE id IN
        <foreach collection="planIds" item="id" open="(" separator="," close="">
            #{id}
        </foreach>
        AND plan_status = '0'
        AND del_flag='0'
    </update>
</mapper>