<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jeecg.modules.scm.mapper.CostCalcMapper">

    <select id="queryProductList" resultType="org.jeecg.modules.scm.vo.CostCalcProductVo">
        SELECT
        p.id AS productId,
        p.product_code AS productCode,
        p.product_name AS productName,
        p.product_spec AS productSpec,
        p.product_color AS productColor,
        p.recipe_id AS recipeId,
        r.recipe_code AS recipeCode,
        r.recipe_name AS recipeName,
        r.version AS recipeVersion,
        r.proportion_total AS proportionTotal,
        r.proportion_type AS proportionType
        FROM mis_product p
        LEFT JOIN mis_recipe r ON r.id = p.recipe_id AND r.del_flag = '0'
        WHERE p.del_flag = '0'
        AND p.recipe_id IS NOT NULL
        AND r.publish_status = '1'
        <if test="productCode != null and productCode != ''">
            AND p.product_code LIKE CONCAT('%', #{productCode}, '%')
        </if>
        <if test="productName != null and productName != ''">
            AND p.product_name LIKE CONCAT('%', #{productName}, '%')
        </if>
        ORDER BY p.create_time DESC
    </select>
</mapper>