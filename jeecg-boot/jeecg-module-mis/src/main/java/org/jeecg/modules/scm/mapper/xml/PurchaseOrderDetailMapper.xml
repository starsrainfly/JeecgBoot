<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jeecg.modules.scm.mapper.PurchaseOrderDetailMapper">

	<delete id="deleteByMainId" parameterType="java.lang.String">
		DELETE 
		FROM  mis_purchase_order_detail 
		WHERE
			 order_id = #{mainId} 	</delete>
	
	<select id="selectByMainId" parameterType="java.lang.String" resultType="org.jeecg.modules.scm.entity.PurchaseOrderDetail">
		SELECT * 
		FROM  mis_purchase_order_detail
		WHERE
			 order_id = #{mainId} 	</select>

	<select id="selectAppliedQtyByOrderId" resultType="java.util.Map">
		SELECT d.source_detail_id AS sourceDetailId, IFNULL(SUM(d.apply_qty), 0) AS appliedQty
		FROM mis_stock_in_detail d
				 INNER JOIN mis_stock_in m ON m.id = d.stock_in_id
		WHERE m.source_order_id = #{orderId}
		  AND m.source_order_type = 'PURCHASE'
		  AND m.approve_status = '0'
		  AND m.del_flag = '0'
		  AND d.del_flag = '0'
		GROUP BY d.source_detail_id
	</select>
</mapper>
