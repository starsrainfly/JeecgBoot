<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jeecg.modules.wms.mapper.InventoryAdjustDetailMapper">

	<delete id="deleteByMainId" parameterType="java.lang.String">
		DELETE 
		FROM  wms_inventory_adjust_detail 
		WHERE
			 adjust_id = #{mainId} 	</delete>
	
	<select id="selectByMainId" parameterType="java.lang.String" resultType="org.jeecg.modules.wms.entity.InventoryAdjustDetail">
		SELECT * 
		FROM  wms_inventory_adjust_detail
		WHERE
			 adjust_id = #{mainId} 	</select>
</mapper>
